import React from 'react'
import './styles.css'

function TodoBtnCreate({text}) {
  return (
    <button 
    className='button' 
    onClick={()=>{
    }}>{text}</button>
  )
}

export default TodoBtnCreate