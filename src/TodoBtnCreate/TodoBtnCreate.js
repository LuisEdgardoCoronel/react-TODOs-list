import React from 'react'
import './styles.css'

function TodoBtnCreate({text}) {
  return (
    <button 
    className='button' 
    onClick={()=>{
    }}>New</button>
  )
}

export default TodoBtnCreate